---
sidebar_label: 与原生交互
sidebar_position: 5
---

# 与原生交互

当 JS 需要调用相机、定位、蓝牙等平台能力，或复用已有原生 UI 时，要通过 **原生模块** 和 **原生组件封装** 打通两端。

## 原生模块 (调原生能力)

**旧方式 (`NativeModules` + Bridge)** ：参数必须可序列化，结果通过回调、Promise 或事件 (`DeviceEventEmitter`) 返回，全程异步。

**新方式 (JSI + TurboModules)** ：同步调用，按需懒加载，配合 Codegen 生成类型安全的接口。

写一个原生模块的大致形态：

iOS (Objective-C)：

```objc
// 注册模块
RCT_EXPORT_MODULE();

// 导出方法给 JS 调用
RCT_EXPORT_METHOD(getDeviceName:(RCTPromiseResolveBlock)resolve
                  rejecter:(RCTPromiseRejectBlock)reject) {
  resolve([[UIDevice currentDevice] name]);
}
```

Android (Java)：继承 `ReactContextBaseJavaModule`，用 `@ReactMethod` 标注要导出的方法。

```java
public class DeviceModule extends ReactContextBaseJavaModule {
  @Override
  public String getName() { return "DeviceModule"; }

  @ReactMethod
  public void getDeviceName(Promise promise) {
    promise.resolve(Build.MODEL);
  }
}
```

JS 侧统一通过 `NativeModules.DeviceModule.getDeviceName()` 调用。

## 原生组件封装 (复用原生 UI)

当某个 UI 必须用原生实现 (如地图、视频播放器、签名板)，可以把原生 View 封装成 RN 组件：

- iOS：实现 `RCTViewManager` 子类，导出原生 View 及其属性。
- Android：实现 `SimpleViewManager`，用 `@ReactProp` 把属性映射到原生 View。
- JS 侧用 `requireNativeComponent` (旧) 或 Codegen 生成的接口 (新) 拿到组件，像普通 RN 组件一样使用。

## 嵌入原生 App (渐进式接入)

已有原生应用想逐步引入 RN，可以把 RN 页面挂载进原生容器：

- iOS：用 `RCTRootView` 加载 JS bundle 并作为一个 `UIViewController` 的根视图。
- Android：用 `ReactRootView` 挂载，托管在原生 `Activity` 里。

:::tip
平台差异用 `Platform.OS` 在运行时判断，或用 `.ios.js` / `.android.js` 文件后缀让打包器自动选对应实现 —— 后者更适合整段逻辑分叉的场景。
:::

## 路由导航

主流方案是 `react-navigation` ，提供三类导航器，可嵌套组合 (如 Tab 里套 Stack)：

| 导航器 | 包 | 说明 |
| --- | --- | --- |
| Stack | `@react-navigation/native-stack` | 页面栈，`native-stack` 用原生导航控件，性能优于 JS 版 |
| Tab | `@react-navigation/bottom-tabs` | 底部标签页 |
| Drawer | `@react-navigation/drawer` | 侧滑抽屉 |

核心 API：`navigation.navigate('Detail', { id })` 跳转传参、`push` 入栈、`goBack` 返回；目标页用 `route.params` 取参数。

:::info
`expo-router` 提供 **文件系统路由** (按目录结构自动生成路由，类似 Next.js App Router)，是 Expo 项目里更省心的选择。
:::

## 参考

1. [RN 原生模块 (iOS)](https://reactnative.dev/docs/legacy/native-modules-ios)
2. [RN 原生组件](https://reactnative.dev/docs/legacy/native-components-android)
3. [react-navigation](https://reactnavigation.org/)

# HTTPS

HTTPS 是安全版本的 HTTP，它并不是一个新的协议，而是在 HTTP 下面增加了一层 SSL/TLS 协议。HTTP 是明文传输的，因此在传输的每一个环节，数据都有可能被第三方窃取或者篡改，也就是中间人攻击。SSL/TLS 协议使用对称加密和非对称加密结合的方式。

- 对称加密：使用同一个秘钥对数据进行加密和解密。
- 非对称加密：使用一对秘钥进行加密和解密，一个是公开的公钥，一个是保密的私钥。用私钥加密的数据，只有对应的公钥才能解密，用公钥加密的数据，只有对应的私钥才能解密。我们可以将公钥公布出去，对方使用我们提供的公钥对数据进行加密，然后我们就可以使用自己的私钥进行解密，这样就能保证数据的安全了。

但是非对称加密有一个缺点就是加密的过程很慢，因此如果每次通信都使用非对称加密的方式的话，反而会造成等待时间过长的问题。因此，SSL/TLS 使用对称加密和非对称加密结合的方式，一开始使用非对称加密的方式来对对称加密的秘钥进行传输，然后以后的通信使用对称加密的方式来加密。

SSL/TLS 四次握手：

1. 浏览器向服务器发送 `client_random` 和加密方法列表。
2. 服务器向浏览器返回 `server_random`，加密方法列表，CA 颁发的数字证书，以及公钥
3. 浏览器验证数字证书，并通过 `client_random` 和 `server_random` 生成 `pre_random`，用服务器的公钥加密后将 `pre-random` 传给服务器。此外，浏览器还会通过 `client_random`， `server_random` 和 `pre_random` 生成 `secret`，后续传输都使用 `secret` 作为密钥进行加密。
   - 其中CA的数字签名是验证证书是否被篡改的关键，它其实就是对证书里面除了CA的数字签名以外的内容进行摘要算法得到一个摘要，然后CA机构用他自己的私钥对这个摘要进行加密就生成了CA的数字签名，CA机构会公开它的公钥，验证证书时就是用这个公钥解密CA的数字签名，然后用来验证证书是否被篡改。
4. 服务器收到公钥加密的 `pre_random`，使用自己的私钥解密，并通过 `client_random`， `server_random` 和 `pre_random` 生成 `secret`，后续传输都使用 `secret` 作为密钥进行加密。



## 参考

1. [3.5 HTTPS 如何优化？ | 小林coding](https://xiaolincoding.com/network/2_http/https_optimize.html)
2. [HTTPS为什么让数据传输更安全？](https://juejin.cn/post/6844904021308735502#heading-84)
3. [Https 是如何保证安全的？ - 知乎](https://zhuanlan.zhihu.com/p/110216210)
4. [对称加密、非对称加密、摘要、数字签名、数字证书 - 掘金](https://juejin.cn/post/6844904072856731662)
5. [浅谈HTTPS🔐 - 掘金](https://juejin.cn/post/7051354995236536357)

